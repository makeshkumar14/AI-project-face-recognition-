# Face Recognition Attendance System - Requirements
# Python 3.9+ required

# Core Flask dependencies
Flask>=2.3.0
flask-cors>=4.0.0
Werkzeug>=2.3.0

# Computer Vision (REQUIRED)
opencv-contrib-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
openpyxl>=3.1.0

# Deep Learning - Advanced Face Recognition
# Note: torch is installed separately to a temp folder to avoid long path issues on Windows
# Run: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu --target "$env:TEMP\torch_temp"
# Then: pip install facenet-pytorch scikit-learn scipy joblib threadpoolctl --no-deps --target "$env:TEMP\torch_temp"

# face_recognition (OPTIONAL - legacy, requires dlib)
# Uncomment below if you can install dlib (requires Visual C++ Build Tools)
# face_recognition>=1.3.0

# Database
# SQLite is built into Python, no external package needed

# Utilities
tqdm>=4.65.0
